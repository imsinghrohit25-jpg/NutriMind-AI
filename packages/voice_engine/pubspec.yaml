name: nutrimind_voice_engine
description: STT/TTS with code-switching, platform + cloud routing, locale-aware persona, wake-word interface.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0
  nutrimind_core:
    path: ../core
  nutrimind_country_engine:
    path: ../country_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
