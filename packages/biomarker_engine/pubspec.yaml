name: nutrimind_biomarker_engine
description: EXISTING lab reports + CGM data handling — unit-system aware (mg/dL ↔ mmol/L).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  nutrimind_core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
