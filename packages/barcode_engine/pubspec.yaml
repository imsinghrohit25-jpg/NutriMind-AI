name: nutrimind_barcode_engine
description: Barcode scan → provider chain resolution. GTIN-13/UPC-A/EAN-8 normalization.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_barcode_scanning: ^0.13.0
  nutrimind_core:
    path: ../core
  nutrimind_food_intelligence:
    path: ../food_intelligence

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
