name: nutrimind_ai_agent_layer
description: LLM orchestration, locale-aware prompts, structured output contracts, PII redaction, cost tracking.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  nutrimind_core:
    path: ../core
  nutrimind_country_engine:
    path: ../country_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
